var colors = require("colors");

console.log("hello".bgCyan);

// === interview question == //

// Q. Is Node.js single-threaded or multithreaded?
// ans is single-threaded because nodejs run one command in one time
